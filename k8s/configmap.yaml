apiVersion: v1
kind: ConfigMap
metadata:
  name: gitcaster-config
data:
  reown-project-id: "your-reown-project-id"
  app-url: "https://gitcaster.example.com"
  redis-host: "redis-service"
  redis-port: "6379"

